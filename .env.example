# ============================================================
# XENO_PROTOCOL Environment Configuration
# Copy this file to .env.local and populate all required values.
# DO NOT commit .env.local to version control.
# ============================================================

# -----------------------------------------------------------
# LLM BACKEND (Required)
# -----------------------------------------------------------
# OpenAI API key for narrative synthesis (GPT-4o-mini)
OPENAI_API_KEY=sk-...

# Model override (optional, defaults to gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# -----------------------------------------------------------
# TELEMETRY APIs (Required for production)
# -----------------------------------------------------------
# Helius RPC endpoint for live Solana telemetry ingestion
# HELIUS_API_KEY=
# HELIUS_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_KEY

# QuickNode Solana RPC (alternative telemetry provider)
# QUICKNODE_RPC_URL=https://your-endpoint.solana-mainnet.quiknode.pro/YOUR_TOKEN/

# WebSocket endpoint for real-time transaction streaming
# SOLANA_WS_URL=wss://mainnet.helius-rpc.com/?api-key=YOUR_KEY

# -----------------------------------------------------------
# DATABASE (Required for production persistence)
# -----------------------------------------------------------
# PostgreSQL connection string (primary relational store)
# DATABASE_URL=postgresql://user:password@localhost:5432/xeno_protocol?schema=public

# Redis connection string (hot-state caching layer)
# REDIS_URL=redis://localhost:6379

# -----------------------------------------------------------
# OBSIDIAN VAULT (Optional, recommended for production)
# -----------------------------------------------------------
# Passphrase for AES-256-GCM encryption of the emotional telemetry vault.
# If not set, falls back to OPENAI_API_KEY-derived key (acceptable for dev).
# Min 32 characters recommended.
# VAULT_SECRET=

# -----------------------------------------------------------
# SECURITY & SESSION (Required for production)
# -----------------------------------------------------------
# Secret for signing JWT session tokens (min 32 characters)
# SESSION_SECRET=

# CORS allowed origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000

# -----------------------------------------------------------
# DEPLOYMENT (Optional)
# -----------------------------------------------------------
# Vercel-specific (auto-set in Vercel environment)
# VERCEL_URL=
# NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Log level: debug | info | warn | error
# LOG_LEVEL=info